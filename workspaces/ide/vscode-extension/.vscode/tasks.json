{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pacwich-vscode-extension: build",
      "type": "shell",
      "command": "bun run build",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
