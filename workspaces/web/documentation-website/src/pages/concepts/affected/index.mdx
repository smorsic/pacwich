---
title: Concepts > Affected Workspaces
titleSuffix: " | pacwich Documentation"
description: "Affected Workspaces | Concepts | Documentation for pacwich: Monorepo tooling on top of Bun, npm, and pnpm workspaces"
pageType: doc
sidebar: true
outline: true
footer: false
---

import { SyntaxHighlighter } from "../../../lib/util/highlight";

# Concept: Affected Workspaces

## Overview

The idea of an **affected workspace** is simply a workspace that can be considered meaningfully changed.

If one can determine which workspaces are affected by some code change, that can be helpful for orchestrating
DevOps tasks, such as running builds for only those changed workspaces.

### What is considered a meaningful change?

By default, a workspace is considered to have a set of **inputs**, the sources of potential chang to a workspace.

When not [configured](/concepts/inputs), the default inputs are:
* All git-trackable files in the workspace's directory
* All [workspace dependencies](/concepts/workspace-dependencies). If a workspace dependency is affected for any reason, its dependents are also considered affected.
* The version resolved in your package manager's lockfile for all other dependencies in a workspace's `package.json`.

Inputs can be configured per script as well. See the [inputs](/concepts/inputs) page for more information.

### How is an affected workspace determined?

There are two main ways to determine affected workspaces:

#### Using git diff (default)

The default behavior of affected features is to use the git diff between the current HEAD and the default base ref in git.

The default base ref is `main` except when overridden via [project configuration](/config/project) or an [environment variable](/config/env-vars).

Uncommitted changes (staged, unstaged, untracked) are considered a part of the diff by default. 
Files that are gitignored are never considered a part of the diff.

<SyntaxHighlighter language="bash">
{`
# Uses diff of current HEAD against the default base ref (main when not configured)
pacwich affected list

# Compare specific refs (can also pass commit SHA, tag, etc.)
pacwich affected list --base=my-branch-a --head=my-branch-b

# Ignore uncommitted changes
pacwich affected list --ignore-uncommitted # (staged, unstaged and untracked)
pacwich affected list --ignore-untracked
pacwich affected list --ignore-unstaged
pacwich affected list --ignore-staged

# Ignore changes to workspace dependencies derived from package.json files
pacwich affected list --ignore-workspace-deps

# Ignore changes to external dependencies (e.g. npm packages)
pacwich affected list --ignore-external-deps

# Run script across affected workspaces with similar options
pacwich affected run my-script --base=my-branch --ignore-uncommitted
`.trim()}
</SyntaxHighlighter>

* See CLI docs for [affected list](/cli/commands#affected-list).
* See CLI docs for [affected run](/cli/commands#affected-run).

#### Using a list of changed files

You can bypass using git entirely and simply pass a list of changed files that match inputs.

<SyntaxHighlighter language="bash">
{`
pacwich affected list --files="tsconfig.json packages/example/**/*.ts"

# Paths with leading / are relative to the project root
pacwich affected run --files="/tsconfig.json packages/example/**/*.ts"
`.trim()}
</SyntaxHighlighter>

#### Debugging

You can use the `--explain` flag to get detailed reasoning for the affected workspaces.

<SyntaxHighlighter language="bash">
{`
# Summary for each affected workspaces
pacwich affected list --explain

# More detailed output about every changed file and dependency
pacwich affected list --explain --detailed
`.trim()}
</SyntaxHighlighter>

## API features

The API and CLI have practical parity for affected features. 

* [FileSystemProject.determineAffectedWorkspaces()](/api/reference#determineaffectedworkspaces)
* [FileSystemProject.runAffectedWorkspaceScript()](/api/reference#runaffectedworkspacescript)