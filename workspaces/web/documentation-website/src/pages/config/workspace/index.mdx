---
title: Workspace Configuration
titleSuffix: " | pacwich Documentation"
description: "Workspace configuration documentation for pacwich | Documentation for pacwich: Monorepo tooling on top of Bun, npm, and pnpm workspaces"
pageType: doc
sidebar: true
outline: true
footer: false
---

import { WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY, CONFIG_LOCATION_TYPES, createConfigLocationPath } from '@pacwich/common/config';
import {  exampleWorkspaceConfigArray, exampleWorkspaceConfigSimple, createPackageJsonExample, createTsFileExample, MERGE_WORKSPACE_CONFIG_EXAMPLE } from "../../../lib/config/exampleConfig";
import { SyntaxHighlighter } from "../../../lib/util/highlight";
import { PROJECT_CONFIG_TYPE, WORKSPACE_CONFIG_TYPE } from "../../../lib/config/configTypes";

# Configuration: Workspace Config File

The optional configuration for a workspace is defined in a file such as <code>{createConfigLocationPath("tsFile", WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY)}</code> in its directory or in its `package.json` file.

You can also use the project config to set workspace configs in bulk by using workspace patterns.
See the [**workspace pattern configs**](/config/workspace-pattern-configs) page for more information.

## Type Definition

<SyntaxHighlighter language="typescript">
  {WORKSPACE_CONFIG_TYPE}
</SyntaxHighlighter>

### Alias

**Aliases** are an alternate name or names for a workspace. See the [workspace aliases](/concepts/workspace-aliases) page for more information.

### Tags

**Tags** are simple strings that can be used to group workspaces together. They can be used
in [workspace patterns](/concepts/workspace-patterns).

### Default Inputs

The `defaultInputs` used to determine which files are considered inputs for the workspace, 
such as for [affected workspaces](/concepts/affected) resolution. These are applied to all 
scripts in the workspace that don't configure their own inputs (see below).

See the [inputs](/concepts/inputs) page for more information.

### Scripts

A map of script names to config specific to any respective script.
Inline workspaces can use an [optional name](/concepts/inline-scripts#script-name) to be matched.

#### Order

The `scripts[name].order` option is used to set the [script execution order](/concepts/script-execution-order) for scripts.

#### Inputs

The `scripts[name].inputs` option is used to set the inputs for the script, such as for [affected workspaces](/concepts/affected) resolution.

See the [inputs](/concepts/inputs) page for more information.

### Workspace Dependency Rules

Using the `rules.workspaceDependencies` field, you can define rules for which workspaces are allowed to be dependencies,
using `allowPatterns`, `denyPatterns`, or both.

[Workspace patterns](/concepts/workspace-patterns) are used to match workspaces.

`allowPatterns` defines the permitted subset of dependencies. `denyPatterns` forbids specific dependencies.
When both are present, `denyPatterns` further filters within the subset permitted by `allowPatterns`.
You can also use [negated](/concepts/workspace-patterns#negation) workspace patterns to exclude workspaces from the allowed or denied list.

For example, you may disallow dependencies with the [tag](/config/workspace#tags) "backend" for a frontend workspace with `denyPatterns: ["tag:backend"]`.

The top-level `allowPatterns`/`denyPatterns` apply to every dependency, transitively, regardless of which `package.json` field
the dependency was declared in.

#### Scoping rules to a dependency field

The `bySource` field scopes rules to specific `package.json` dependency fields (`dependencies`, `devDependencies`,
`peerDependencies`, `optionalDependencies`), keyed by field name. Each entry uses the same `allowPatterns`/`denyPatterns` shape and
applies **in addition to** the top-level rule, so a dependency declared in that field must satisfy both.

A field-scoped rule covers the workspace's directly-declared dependencies in that field **and everything those dependencies pull in
transitively**, so a forbidden workspace cannot leak in through a permitted field-scoped dependency. For example,
`bySource: { devDependencies: { allowPatterns: ["tag:test-util"] } }` restricts dev dependencies (and their transitive deps) to test
utilities, while leaving production `dependencies` governed only by the top-level rule.

[Workspace dependencies](/concepts/workspace-dependencies) are explicitly defined in workspaces' `package.json` files.

## Examples:
### 1. TypeScript/JavaScript file

<span className="config-path"><code>path/to/your/workspace/{createConfigLocationPath("tsFile", WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY)}</code></span>
or
<span className="config-path"><code>path/to/your/workspace/{createConfigLocationPath("jsFile", WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY)}</code></span>

<SyntaxHighlighter language="typescript">
  {createTsFileExample(exampleWorkspaceConfigSimple, "workspace")}
</SyntaxHighlighter>

#### `mergeWorkspaceConfig` Utility
You can use the `mergeWorkspaceConfig` function to merge multiple workspace configurations left to right, with each subsequent config taking precedence. 
Objects are merged deeply, and array items are concatenated while keeping items unique.

<SyntaxHighlighter language="typescript">
  {MERGE_WORKSPACE_CONFIG_EXAMPLE}
</SyntaxHighlighter>

### 2. JSON/JSONC file

<span className="config-path"><code>path/to/your/workspace/{createConfigLocationPath("jsonFile", WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY)}</code></span>
or
<span className="config-path"><code>path/to/your/workspace/{createConfigLocationPath("jsoncFile", WORKSPACE_CONFIG_FILE_NAME, WORKSPACE_CONFIG_PACKAGE_JSON_KEY)}</code></span>

<SyntaxHighlighter language="json">
  {JSON.stringify(exampleWorkspaceConfigSimple, null, 2)}
</SyntaxHighlighter>

### 3. package.json

<span className="config-path"><code>path/to/your/workspace/package.json</code></span>

<SyntaxHighlighter language="json">
  {JSON.stringify(createPackageJsonExample(exampleWorkspaceConfigSimple, "workspace"), null, 2)}
</SyntaxHighlighter>
