# Workspace: @pacwich/documentation-website

This uses the rspress framework for the documentation website,
an MDX-based framework similar in goal to Docusaurus on top of rspack.

The build is created via `scripts/build.ts`.

## Feature addition/change checklist:

- CLI:
  - rspress config:
    - nav items
    - sidebar items
  - Quick start example
  - Global option/command reference
- API:
  - rspress config:
    - nav items
    - sidebar items
  - Quick start example
  - Reference example
- Concepts:
  - rspress config:
    - nav items
    - sidebar items
  - Cross-linking with concepts, CLI, API
- Config:
  - rspress config:
    - nav items
    - sidebar items
  - Cross-linking with concepts, CLI, API
