/**
 * Shared, rspress-flavored CSS for pacwich's web workspaces
 * (`documentation-website`, `web-cli`). No JS/TS module exports today — the
 * CSS files under `css/` are consumed directly via subpath imports, e.g.
 * `@pacwich/web-common/css/web-cli.css`.
 */
export {};
